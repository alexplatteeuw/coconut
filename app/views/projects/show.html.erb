<div class="container">
<header>
  <div class="overlay"></div>
  <video playsinline="playsinline" autoplay="autoplay" muted="muted" loop="loop">
    <source src=<%= @project.video_url %> type="video/mp4">
  </video>
  <div class="container h-100">
    <div class="d-flex h-100 text-center align-items-center">
      <div class="w-100 text-white">
        <h1 class="display-5"><%= @project.name %></h1>
        <p class="lead text-white mb-0">👉 <%= @project.charity.name %> <%= image_tag @project.image_url, height: 50, crop: :fill %></p>
      </div>
    </div>
  </div>
</header>

  <div class="row justify-content-around">

    <div class="col-6">
      <div class="mt-3">
        <h3>Description:</h3>
        <div class="border rounded bg-white">
          <p><%= @project.description%></p>
        </div>
      </div>
    </div>

    <div class="col-3">
      <div>
      <div class="mt-3">
        <h3>Compétences:</h3>
      </div>
      <div class="border text-center rounded">
        <ul>
          <% @project.skills.each do |skill| %>
          <li>
            <%= skill.name %>
          </li>
          <% end %>
        </ul>
      </div>
      <% if current_user.credits.nil? || current_user.credits == 0 %>
      <div class="container">
        <p class="mt-5 btn btn-flat btn-primary btn-lg">
          Plus de crédit!
        </p>
      </div>
      <% else %>
        <div class="container">
          <%= link_to 'Je veux participer !', new_project_reservation_path(@project), class: 'mt-5 btn btn-flat btn-primary btn-lg' %>
        </div>
      <% end %>
    </div>
  </div>
</div>




  <%= render 'projects/employees', employees: @employees %>

</div>

