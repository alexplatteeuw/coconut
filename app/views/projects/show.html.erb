<div class="container">
  <h2><%= @project.name %> </h2>
  <p><%= @project.description%></p>
  <h3>Skills needed:</h3>
  <ul>
    <% @project.skills.each do |skill| %>
    <li>
      <%= skill.name %>
    </li>
    <% end %>
  </ul>
</div>
