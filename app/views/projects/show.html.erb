<div class="container">
<header>
  <div class="overlay"></div>
  <video playsinline="playsinline" autoplay="autoplay" muted="muted" loop="loop">
    <source src=<%= @project.video_url %> type="video/mp4">
  </video>
  <div class="container h-100">
    <div class="d-flex h-100 text-center align-items-center">
      <div class="w-100 text-white">
        <h1 class="display-5"><%= @project.name %></h1>
        <p class="lead text-white mb-0">👉 <%= @project.charity.name %> <%= image_tag @project.image_url, height: 50, crop: :fill %></p>
      </div>
    </div>
  </div>
</header>

  <div class="row justify-content-around">

    <div class="col-6">
      <div class="mt-3">
        <h3>Description:</h3>
        <div class="border rounded bg-white">
          <p><%= @project.description%></p>
        </div>
      </div>
    </div>

    <div class="col-3">
      <div class="mt-3 container widget-content" data-unavailable="<%= @user.unavailable_dates.to_json %>">
        <%= simple_form_for [@project, @reservation] do |f|%>
        <%= f.input :start_date, as: :string, required: false, input_html: {class: "datepicker", id: "range_start"} %>
        <%= f.input :end_date, as: :string, required: false, input_html: {class: "datepicker", id: "range_end"} %>
        <%= f.button :submit, "Réserver", class: "mt-2 btn btn-flat btn-primary btn-lg" %>
      <% end %>
      </div>
      <div>
      <div class="mt-3">
        <h3>Compétences:</h3>
      <div class="border text-center rounded">
        <ul>
          <% @project.skills.each do |skill| %>
          <li>
            <%= skill.name %>
          </li>
          <% end %>
        </ul>
      </div>
        <%= render 'projects/employees', employees: @employees %>
    </div>
  </div>
</div>

</div>
