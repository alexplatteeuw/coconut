<% unless current_user.nil?%>
<div id="sidebar">
  <div id="sidebar-container">

    <div class="logosb">
      <%= link_to 'COCONUT', root_path %>
    </div>

    <div class="d-flex justify-content-center">
      <%= image_tag current_user.avatar_url, id: "avatar" %>
    </div>

    <div class="fname d-flex justify-content-center">
      <p><%= current_user.first_name%></p>
    </div>
     <div class="fname d-flex justify-content-center">
      <p>-<%= current_user.job%>-</p>
    </div>


      <ul class="sidebar-brand">
        <li>
          <i class="icons pr-3 fa fa-list" aria-hidden="true"></i><%= link_to 'Tous les projets', projects_path %>
        </li>
        <% if current_user.admin? %>
          <li>
            <i class="icons pr-3 fa fa-check" aria-hidden="true"></i><%= link_to 'Projets retenus', favorites_path %>
          </li>
        <% else %>
          <li>
            <i class="icons pr-3 fa fa-bookmark-o" aria-hidden="true"></i><%= link_to 'Mes projets', my_projects_path %>
          </li>
        <% end %>
        <% if current_user.admin? %>
          <li>
            <i class="icons pr-3 fa fa-users" aria-hidden="true"></i><%= link_to 'Employés', root_path %>
          </li>
        <% end %>
        <li>
          <i class="icons pr-3 fa fa-ellipsis-h" aria-hidden="true"></i><%= link_to 'Mes documents', root_path %>
        </li>
        <li>
          <% if current_user %>
          <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
          <% else %>
          <%= link_to "Sign in", new_user_session_path %>
          <% end %>
        </li>
      </ul>
  </div>
</div>




  <div>
    <button type="button" id="sidebarCollapse" class="btn btn-secondary">
      <i class="fas fa-align-left"></i>
    </button>
  </div>

<% end %>
