<div class="cards">
  <% unless projects.empty? %>
    <% projects.each do |project| %>
      <%= render "shared/card-project", project: project %>
    <% end %>
  <% else %>
  <div class="pt-3">
    <em>Aucun projet à afficher !</em>
  </div>
  <% end %>
</div>
